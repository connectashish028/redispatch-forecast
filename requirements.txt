# Streamlit Cloud reads this file. Keep it MINIMAL — only what
# `streamlit run app.py` and the modules it imports need at runtime.
# Dev/test/training extras are in requirements-dev.txt.

# Data IO
pandas>=2.0,<3.0          # pinned <3.0; pandas 3.x is bleeding-edge
numpy>=1.24,<3.0
pyarrow>=10                # parquet reads

# Dashboard runtime
plotly>=5.18
streamlit>=1.30

# Calibrator unpickling for the "Why?" panel. The dashboard has a try/except
# fallback to uncalibrated sigmoid if these are absent, but predictions
# read better with calibration.
scikit-learn>=1.6,<1.7    # pinned to match training-time calibrator pickle
joblib>=1.3
