# Core data + IO (v1 + v2)
pandas>=2.0
numpy>=1.24
pyarrow>=10                # parquet IO
requests>=2.28             # SMARD + Open-Meteo clients

# Visualization / dashboard (v1)
plotly>=5.18
matplotlib>=3.7
folium>=0.15
streamlit>=1.30
streamlit-folium>=0.15

# Notebook
jupyter
ipykernel

# Testing
pytest>=7.0

# v2 prediction stack (optional; only needed to run anything under src/v2/)
lightgbm>=4.0
scikit-learn>=1.3
joblib>=1.3
holidays>=0.40
