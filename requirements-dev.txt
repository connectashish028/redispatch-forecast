# Dev / training / notebook extras.
# Install with:  pip install -r requirements.txt -r requirements-dev.txt

# Data fetching (smard_client, weather_client, fetch_shn_ops, fetcher)
requests>=2.28

# v2 ML training + scoring (src/v2/train.py, src/v2/score_today.py).
# The dashboard itself doesn't need these.
lightgbm>=4.0
holidays>=0.40            # used by build_features.py for is_de_holiday

# Animation GIF generator (tools/render_animation_gif.py)
kaleido<1                  # plotly -> static PNG export
imageio>=2.30              # PNG -> GIF stitching

# Notebook
jupyter
ipykernel
matplotlib>=3.7

# Tests
pytest>=7.0
